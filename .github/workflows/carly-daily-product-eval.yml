name: Carly daily product eval

on:
  push:
    branches:
      - ops/carly-daily-eval
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: carly-daily-product-eval
  cancel-in-progress: true

jobs:
  evaluate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Verify v43 production runtime is reachable
        run: |
          python - <<'PY'
          import json, time, urllib.request
          expected='commercial-v43-stage-timing'
          last=None
          for _ in range(18):
              try:
                  with urllib.request.urlopen('https://cartrade-resolver.onrender.com/carly/runtime', timeout=20) as r:
                      last=json.loads(r.read().decode())
                  print(last)
                  if last.get('composition') == expected:
                      break
              except Exception as exc:
                  print(type(exc).__name__, exc)
              time.sleep(5)
          else:
              raise AssertionError(last)
          PY
      - name: Run representative buyer conversations and capture latency
        run: python tests/daily_carly_product_eval.py
